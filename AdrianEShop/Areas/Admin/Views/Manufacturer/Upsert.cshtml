@model AdrianEShop.Models.ViewModels.Manufacturer.ManufacturerUpsertVM

<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="col-12 border-bottom">
            <h2 class="text-primary">@Model.PageTitle</h2>
        </div>
        <div class="col-8 pt-4">
            @if (Model.Manufacturer.Id != Guid.Empty)
            {
                <input type="hidden" asp-for="Manufacturer.Id" />
            }
            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="@Model.Manufacturer.Name"></label>
                </div>
                <div class="col-8">
                    <input asp-for="@Model.Manufacturer.Name" class="form-control" />
                    <span asp-validation-for="@Model.Manufacturer.Name" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-8 offset-4">
                    @if (@Model.Manufacturer.Id != Guid.Empty)
                    {
                        <partial name="_EditAndBackToListButton" model="@Model.Manufacturer.Id" />
                    }
                    else
                    {
                        <partial name="_CreateAndBackToListButton" />
                    }
                </div>
            </div>
        </div>
    </div>
</form>